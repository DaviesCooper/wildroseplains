import path from 'path';

export const orderDataDir = path.resolve('data/orders');

export const staticDir = path.resolve('public');

export const indexFilePath = path.join(staticDir, 'index.html');

