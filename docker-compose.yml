version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: wildroseplains:local
    ports:
      - "8080:8080"
    env_file:
      - backend/.env
    environment:
      NODE_ENV: production
      PORT: 8080
      FRONTEND_ORIGINS: http://localhost:8080

